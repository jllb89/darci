A couple of mapping choices worth calling out:

Durable if recorded maps to durability_rule = 'conditional'
opaque or incomplete acknowledgment/witnessing rules stay machine-readable where possible, but still keep the original legal text alongside them
everything is marked needs_review so the system can use it, but nobody mistakes it for final legal approval


call notes:

illuminotary - 
member
pro role - generates document for another person to sign  and has permission to view the other person's documents
member - document owner
trust registry - one way we generate documents inside the platform vs documents being uploaded. 8 steps generate 2 documents
stamp dataset is the same we will request for illuminotaries

In‑person verification notified when sharing the code