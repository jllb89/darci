# Phase 1 execution — 17 September 2026

Status: **in progress; not approved for deployment or production**. Baseline `4e7f3ff91d9a6078525e8c6562ebe5b94432db51`.

## Approved decisions

- Hash-only launch: genuine SHA-256 checks; no external-ledger claim. Real anchoring deferred.
- Protect identity data now. No automatic identity deletion or invented retention period; legal holds and approved policy still required.
- Recovery goals: RPO 24 hours, RTO 4 hours. Approved up to $15/month total for isolated recovery resources. On 18 September Jorge also approved short-lived automatic AWS backups; no always-on server or paid database/test instances.
- Critical alert responder: Jorge, `lopezb.jl@gmail.com`, **sole responder for now**, explicitly requested on 18 September. No secondary coverage is claimed; this operational limitation remains visible.
- Public verification remains available by link. PDF previews/downloads belong only inside the authenticated, authorized app, subject to existing billing holds. The public implementation returns status/hash only: hiding a download button on a public PDF viewer would not protect the PDF bytes.
- Sentry provider configuration/rules/symbolication **deferred at Jorge's request on 18 September**. No further Sentry access request is pending. Existing integrations/redaction are preserved; deferred provider acceptance is not marked passed. Other critical alert work remains required.
- Provisioned the approved `darci-recovery` stack in AWS account ending 3951, us-east-1: private versioned bucket, KMS key, separate scoped writer/reader roles, SNS route and budget notifications. Budget notifications are not a hard spending cap.
- SNS subscription confirmed by Jorge. On 17 September Jorge confirmed receipt of both the AWS recovery-failure alert and the controlled Resend recovery test email. This proves those delivery routes, not every critical failure detector.
- Supabase CA certificate supplied in Downloads and valid through April 2031. Verified database TLS succeeded via the project's already-linked IPv4 session pooler; the direct database endpoint is IPv6-only.
- Explicitly approved verification-only correction of existing beta simulated receipts: add audited SHA-256 evidence only after exact-byte and native PDF checks. Do not change PDFs, signatures, acknowledgments, existing hashes, old receipts or billing holds.
- **Production data decision:** launch with fresh accounts/documents; preserve beta in its separate environment. Do not migrate beta accounts, documents or test subscriptions; do not delete beta records. Historical beta PDF exceptions remain a separate review, not seed data for production.

## Implementation in the working tree

- Verified intended-recipient invite claim RPC; invite/token/claim/audit transaction and idempotency. Anonymous, wrong-email, unverified, expired and revoked claims fail closed.
- Hash-only `not_required` ledger disposition, separate `isFinalized` versus `isAnchored`, client completion/copy changes, rejection of historical simulated receipts as proof. Missing file bytes can no longer produce a document hash.
- Finalization uploads request non-overwrite storage and read-back equality. Native PDF processing gains input/page/concurrency limits. Further crash/partial-package recovery proof remains required.
- Explicit Stripe test/live configuration in application code and a forward SQL migration with a database environment boundary. Live activation stays off. Full migration/lifecycle/concurrency acceptance remains required.
- Protected AES-256-GCM identity-value storage and transaction RPC; no full identifier copied to new general identity/check-in metadata. No plaintext fallback. Real SQL role/actor/envelope/metadata/audit-rollback and exact provenance tests pass. Staging key prepared in the existing AWS secret; legacy protection/backfill and legal-hold acceptance remain required.
- Required material-state audit triggers; full acceptance and immutable-provenance work remain underway.
- Public error sanitization, token-path/Sentry redaction, shared Redis rate limits, API/browser safety headers compatible with blob PDF previews, readiness and worker heartbeat implementation. Alert installation and delivery exercises not yet completed.
- Removed a service-role escalation path through profile metadata: only the signed top-level service credential bypasses application identity lookup. Added negative tests for user/app metadata requesting service privileges and a positive service-credential case. Not deployed yet.
- Docker runtime moves from Node 20 to Node 24/Alpine 3.24, with production dependency pruning, no runtime npm and non-root API/worker/web. Required PDF substitute fonts and writable temporary font cache are included. Rebuilt API, worker and web `phase1-19` candidates include the public-verification edits and have **zero HIGH/CRITICAL findings** in Trivy 0.74.0; no ignore list was used. Reports: `/private/tmp/darci-phase1-{api,worker,web}-trivy19.json`. Earlier Debian candidates had unresolved OS findings and were replaced, not waived. These local synthetic-config candidates are not an approved/deployed production release.
- Authentication no longer reactivates revoked roles from the legacy profile field. Signed anonymous/non-user tokens are rejected. Production/staging enforce issuer, audience, expiration, current app grants and a live owned Auth session; missing-profile/test-flag bypasses cannot reopen access. Logout remains reachable. These changes are local, not deployed.
- Billing human support mutations now require signed `aal2` plus a recent TOTP AMR entry. Token refresh/`iat`/an invented `auth_time` do not count as MFA. Operator enrollment/UI acceptance and other privileged-action controls remain outstanding.
- CI now includes isolated SQL/Storage/Auth tests and iOS unit/UI tests. Staging deployment depends on CI for the same revision, scans the actual pushed images, preserves their digests and deploys by those verified digests. A read-only preflight stops deployment if the new database capabilities or encryption configuration are missing. Workflows pass actionlint locally; no GitHub run/deployment is claimed.

## Evidence to date (local, not deployed)

- Disposable PostgreSQL 16 invite transaction test: 20 concurrent calls produced one claim, token consumption and audit entry. Injected audit failure rolled back; retry succeeded. Negative identity/expiry/revocation/RPC-grant cases passed. This is **not** a full Supabase migration/RLS rehearsal.
- Focused backend regression run: 55/55 tests, including native PDF tools. Log `/private/tmp/darci-phase1-unit.log`.
- New security, Stripe environment, telemetry redaction and encryption primitive tests: 14/14 passed before subsequent edits.
- Web standalone typecheck passed after fixing the existing obsolete `government_id` assertion. Repeat after all edits.
- Backend/web dependency upgrades resolved the inspected npm advisories to zero at the recorded install/audit steps. Subsequent final-image HIGH/CRITICAL scans also pass as recorded above. PostgreSQL 17 client tools are installed.
- Backend compiler and **614/614 tests across 92 files** pass after the public-verification changes (`/private/tmp/darci-phase1-backend-build19.log`, `/private/tmp/darci-phase1-backend-tests20.log`). Observability validation previously passed 44 catalog entries, 43 emitted codes, eight declared alert rules and five owners. Declared rules are not proof of installed provider alerts. Ordinary Vitest commands no longer load `.env.staging` credentials.
- Clean disposable Supabase installation now passes **all 101 migrations**, followed by the actual finalization, identity/provenance, Auth session/API, legacy evidence and Storage boundary suites (`/private/tmp/darci-phase1-migration-reset8.log`, `/private/tmp/darci-phase1-migrated-boundaries8.log`). This is not complete runtime/authorization acceptance.
- Populated upgrade rehearsal passes against the exact second recovered beta archive, with outbound networking disabled. The eight missing Phase 1 migrations install; the already-applied legacy evidence migration is preserved. Whole-row fingerprints across **16 evidence/billing tables remain identical**, including signatures, acknowledgments, hashes, receipts, release controls, identity records and audit events. Candidate SQL digests are recorded in the private report. Log `/private/tmp/darci-phase1-populated-upgrade8.log`; no source database or PDF was changed.
- Real SQL finalization tests passed: twelve concurrent output retries and twelve completion retries; wrong actor/unprivileged RPC denial; partial/duplicate package rejection; injected audit failure rolls back output; immutable hash; atomic completion leaves release `pending` until billing evaluation.
- Both existing billing SQL suites now pass against the clean schema through `test-phase1-billing-sql.mjs`: allowance/duplicate use/reversal, hold/release state, member/unrelated/assigned-notary RLS and webhook lease behavior. The runner adds explicitly synthetic catalog prerequisites inside rolled-back transactions; it never calls Stripe or creates staging evidence. Additional live/invalid/missing environment and configuration-grant negatives pass. CI includes this runner; log `/private/tmp/darci-phase1-billing-sql8.log`. The six missing actual Stripe lifecycle categories remain open.
- Real local Storage API tests passed: anonymous/member direct download, signing, listing and deletion denied; server-authorized signed download/upload preserved with exact PDF checksum. Direct protected-identity SELECT/UPDATE/DELETE grants denied for anon/authenticated/service roles. The full application role/hold matrix remains required.
- Recovery script and [runbook](production-recovery-runbook.md) are present. First attempts failed closed on missing CA and IPv6 connectivity; no false complete manifest was accepted. The retry uses verified TLS, a consistent read-only database snapshot and actual Storage object bytes.
- Web: **72/72 tests across 14 files** and production build pass after the public-verification changes (`/private/tmp/darci-phase1-web-tests19.log`, `/private/tmp/darci-phase1-web-build19.log`); lint has zero errors and one pre-existing hook warning. iOS simulator build and 110/110 unit tests previously passed, with no iOS changes in this follow-up. Full UI suite passed **12/12** (`/private/tmp/darci-phase1-ios-ui7.log`); corrected ambiguous keyboard/action selectors and an outdated pre-session heading expectation, without weakening the flow assertions.
- Actual local Auth/API test passes: genuine Supabase login accepted, public anon key rejected, revoked role denied, and the same still-unexpired token rejected after real Auth logout. SQL tests additionally prove session ownership, expiry, banned/deleted users and server-only RPC grants. Supabase documents the session check and signed AMR fields in [sessions](https://supabase.com/docs/guides/auth/sessions) and [JWT claims](https://supabase.com/docs/guides/auth/jwt-fields).
- Both real ARM64 runtime images passed non-root/workspace-dependency startup checks. The API image passed native normal/protected-PDF processing and malformed rejection with network disabled, read-only filesystem and resource limits. Images were not pushed or deployed.
- Final web image also passed network-isolated, non-root startup checks: `/`, `/app/billing` and `/privacy` returned HTTP 200. The disposable web container was stopped afterward. This does not assert an authenticated browser billing flow.

## Recovery receipt and remaining exceptions

- Snapshot: `2026-09-17T21-36-33.579Z-11801b53-a4be-491d-a4a5-3a45d769cfa7`.
- Exact manifest: `snapshots/2026-09-17T21-36-33.579Z-11801b53-a4be-491d-a4a5-3a45d769cfa7/manifest.json`, S3 version `2vSZxRmaIs.ESq2d9LuaDMID1KaDoRUx`.
- Database archive 12,317,295 bytes; 2,474 actual objects; combined 698,164,414 bytes. Snapshot duration 1,261 seconds.
- Independent reader-role restore: **2,474/2,474 exact length/SHA-256 matches**. Native checks: 2,346 readable PDFs, 50 PDFs requiring review, 78 other objects. All 50 exceptions have matching backup checksums: the backup preserves pre-existing source defects. The object drill deliberately reports **not accepted**, not a clean bill of health.
- Offline PostgreSQL 17 restore succeeded with outbound networking disabled. All 2,474 database/object inventory identities and versions match the manifest; **229/229 completed document hashes match actual backed-up bytes**. Restored 765 documents, 2,407 versions, 30 Auth users and 17,245 audit events. One application identity has no matching Auth user and requires separate review.
- Repeatable DB-only restore took 2.688 seconds on the warm local runtime; object download/check took 614.552 seconds. These are component measurements, **not** proof of full application RTO or a scheduled 24-hour RPO. Original DB role ownership/passwords, Supabase services, key recovery, private/held authorization and safe worker reconstruction still need acceptance.
- Private reports remain in the operator's temporary recovery directory, outside Git; do not attach customer PDFs, database dumps or object inventories to tickets.
- Second snapshot includes the 194 hash-only attestations and the newly prepared identity key: `2026-09-17T22-44-12.514Z-c282cc86-f1c5-4c69-ac82-8b0af5a4b490`, exact manifest version `wfGjUnvvq27tUcejjPAfpjEpNMsKwEtz`. Database plus objects: 698,244,726 bytes, with the small encrypted key artifact additional; duration 293.02 seconds. Future manifests include the key file in the total-byte counter.
- Independent second reader-role restore again gives **2,474/2,474 exact object checksums**, 2,346 readable PDFs and the same 50 source exceptions, in 651.457 seconds. Identity-key artifact downloaded and checksum-checked. The object report still correctly fails full readability acceptance; no PDF was rewritten. Fresh production does not inherit these records.
- Second offline DB component restores 765 documents, 2,407 versions, 30 Auth users, 2,474 Storage records and **17,439 audit events**. Again **229/229 completed hashes** match backed-up bytes; the same single unmatched app/Auth identity remains flagged. DB-only time 2.696 seconds, not whole-application RTO.
- Existing staging secret version `531ab59c-817f-4abc-951c-606cfa839a56` adds only the 256-bit identity key and key ID; all other values were preserved. No service was restarted and protected writes are not activated yet. The key must never be rotated casually while encrypted records depend on it.
- Installed `darci-recovery-staging-snapshot-success-missing`: missing/zero successful snapshots over 20 hourly evaluations warns before the 24-hour RPO. Controlled ALARM injection executed its SNS action successfully; actual metric evaluation returned it to OK and delivered the recovery action. **This alarm does not schedule backups.**

## Approved beta hash-only correction — applied

- Read-only preflight checked 202 legacy final files: 194 readable/exact-hash matches; eight native PDF check failures. Apply repeated every check before writing evidence: **194 audited attestations added; eight left unverified for review**. No source PDF, signature, acknowledgment, existing hash, old receipt or billing release control changed.
- Only additive migration `20260917140000_legacy_hash_reverification.sql` was applied to staging, with its migration-history entry. The seven earlier Phase 1 migrations and the later `20260917150000_auth_session_liveness.sql` remain local. A later migration timestamp is **not** evidence that other migrations were deployed. Review the complete missing set before the eventual staged rollout (`--include-all` may be needed); never mark unapplied migrations as applied. The new deployment preflight was tested against current staging and correctly refused rollout.
- Actual local SQL proof: anon/authenticated/service-role cannot invoke correction RPC or mutate attestations; wrong hash/object version/size/page count rejected; injected audit failure rolls back; ten concurrent retries produce one attestation and audit; records immutable; original evidence unchanged.
- Backend read paths recognize the new evidence as hash-only and suppress simulated transaction/time/anchor claims. These application changes are **not deployed** yet. Readability failures remain unverified; the correction does not repair them or approve production.

## Email callback recovery — applied and delivered

- Found two disabled Resend webhooks: the `.dev` endpoint had a different signing secret; the old `.com` endpoint matched the deployed AWS secret.
- Updated matching webhook `10330e45-654f-4de9-a5e7-5e6822027947` to `https://api.staging.darciregistry.dev/webhooks/resend` and enabled it, preserving its events and signing secret. Duplicate `b85904a4-6f0c-4380-b310-3f451e1b2a11` remains disabled; nothing was deleted and no application redeploy was required for this repair.
- Signed synthetic unmatched-delivery probe returned expected HTTP 202. One idempotent, clearly labeled test email was sent only to the approved on-call address. Provider message `01a0b172-23ff-767b-9bcc-d6eeda27c9c6` reports `delivered`; `email.sent` and `email.delivered` callback attempts both report `success` (22:17:24 UTC). Jorge confirmed receiving the email and AWS recovery alert.
- This is provider delivery/callback proof, not full notification-outbox/auth OTP/bounce/suppression lifecycle acceptance. The controlled message had no customer document or login token. Critical-category detectors and escalation/recovery drills remain outstanding. Sentry is explicitly deferred; OTLP is unverified. Jorge has chosen sole-responder coverage for now.

## Public verification privacy — implemented locally, not deployed

- `/api/verify/:idn` uses an explicit allowlist: document IDN, verification state and SHA-256 hash. Legacy compatibility fields contain null anchor data and an empty document list; filenames, PDF bytes and signed Storage URLs cannot pass through, including for a request with an Authorization header.
- The service no longer mints public download URLs. Existing exact-byte/readability validation is preserved. Missing or unreadable evidence must not produce a verified result.
- `/verify/[id]` displays the verification result and fingerprint, no document preview, and a sign-in link. Authenticated access alone is not enough: existing in-app document authorization and held-package policy still apply.
- Anonymous/auth-header backend leakage regressions and web allowlist/malformed-hash/error/loading tests pass. Browser QA against a synthetic fixture containing a deliberately rejected legacy signed URL confirms the status/hash-only page.
- This is not deployed. URLs previously issued by the old API remain usable until their existing expiry; already retrieved PDF bytes cannot be recalled. A public preview with its download button hidden would still expose those bytes.
- Rebuilt web-image startup proof: `/verify/PUBLIC123456` returns HTTP 200 as UID 1000 with a read-only filesystem and networking disabled (`/private/tmp/darci-phase1-web-runtime19.log`). This tests the rendered shell, not a live authenticated workflow.

## Automatic AWS backup — enabled and cloud dispatch verified

- Approved recovery stack extended with a dedicated private ECR repository, KMS-protected source-secret subset, no-ingress security group, short-lived non-root/read-only Fargate task, 30-day runtime logs, scheduler role/group, encrypted failure queue and its alarm. No always-on server, paid test database, app redeployment or source-data modification.
- Recovery image `427057633951.dkr.ecr.us-east-1.amazonaws.com/darci-recovery@sha256:0f2e7d61f25c0156d23dbdfaffbb85a953c0e2fd26557f21ab287cac858d2f3d` passed non-root/read-only smoke checks and **zero HIGH/CRITICAL** findings before receiving credentials. Temporary local ECR login removed after upload.
- Source subset version `dd9e27fc-ec88-4c00-a19f-950561eb0fcb` references app-secret version `531ab59c-817f-4abc-951c-606cfa839a56`. It contains database/Storage and identity-key recovery settings only. App secret unchanged; no Stripe/email/SMS keys copied. Underlying source DB/Storage credentials remain privileged, and rotations require an explicit subset refresh and recovery check.
- An actual one-time EventBridge Scheduler dispatch, using the same target/role/network as the recurring schedule, launched ECS task `259497f4ba5941f59fc4336fd4257f5f`. It exited **0**, after **306.311 seconds**, with **2,474 objects and 698,244,839 bytes** (database, objects and identity-key artifact).
- Snapshot `2026-09-18T18-19-47.374Z-ad461874-e43d-4f2a-9e37-fbf3d34bee48`; exact manifest `snapshots/2026-09-18T18-19-47.374Z-ad461874-e43d-4f2a-9e37-fbf3d34bee48/manifest.json`, version `zaCDB49xQUBjK7LEEgwY.26E21i15liS`.
- Independent recovery-reader check downloaded that exact encrypted manifest and checksum-verified the new database archive and identity-key artifact; archive index is valid. **All 2,474 object references pin the same exact key/version/hash/size already downloaded and checked in the second restore.** This is not a new full object-rendering/application restore; the same 50 source-PDF exceptions remain. Private receipt artifacts stay outside Git.
- Recurring schedule is `darci-recovery / darci-recovery-staging`, **ENABLED**, 02:00 and 14:00 America/Mexico_City, task definition `darci-recovery-backup:1`. Enabled only after the real scheduler dispatch and independent receipt checks passed; CloudFormation and Scheduler read-back confirm activation. The next regular slot is 18 September at 14:00 Mexico City time; its result is not yet observed. Both source-data access and copies are staging-only.
- CostCenter allocation activated and the existing $15/month budget now filters recovery-tagged resources. Notifications are not a cap or proof that every shared/untaggable charge is captured; review total AWS recovery costs plus source-provider transfer. No retention deletion was enabled.
- Task failures route through EventBridge → encrypted SQS → CloudWatch alarm → the already-confirmed SNS email route. The queue policy restricts the event source to this account's recovery rules. Direct EventBridge-to-SNS conditions were replaced because [AWS does not support Condition blocks for that SNS integration](https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-use-resource-based.html).
- Controlled failure task `919fc2bd863f40f7b2d3787038ca4047` intentionally exited **1** at the configuration guard, before source configuration/database access. Its genuine ECS failure event reached SQS, the alarm transitioned to **ALARM**, and CloudWatch recorded a successful SNS action at **2026-09-18 18:29:23.878 UTC**. The recipient had previously confirmed this SNS route, but this particular email has not been separately human-acknowledged. Only the exact synthetic task's queue message is acknowledged after this proof; unrelated failures and backup artifacts are preserved.
- Post-exercise queue inspection reports **zero visible and zero in-flight messages**. At the last check the failure alarm still retained its five-minute MAX window's test datapoint; automatic return to OK has not yet been observed. It was not manually cleared. The missing-backup alarm remains OK. Do not count this as every recovery/escalation case being exercised.
- Four local infrastructure regression tests pass, covering opt-in/digest-pinned scheduling, scoped task/secret permissions, non-root/read-only/no-ingress runtime, retained append-only evidence and supported restricted failure routing. CI now runs `node --test ../infra/recovery/stack.test.mjs`; this is local proof, not a claimed GitHub execution.

## Required before closing Phase 1

The 1A–1E exit criteria remain authoritative in [the production roadmap](production-readiness-roadmap-2026-09-17.md), with Sentry acceptance explicitly deferred and sole-responder coverage recorded. Remaining work includes full role/held-access and privileged-operator acceptance; legacy identity protection/legal holds; full product/package crash recovery; ongoing regular-schedule evidence and whole-application recovery; six missing Stripe lifecycle categories and outage/concurrency exercises; remaining critical-category detectors and outbox/OTP/bounce/suppression delivery proof; reviewed source revision, actual CI and staging deployment. Clean and populated **database** migration rehearsals now pass; deployed runtime compatibility still needs acceptance. Fresh-production provisioning remains a later phase.

All four questions in the latest decision batch are answered: automatic recovery approved within the existing total budget; public verification retained without public PDFs; Sentry deferred; Jorge is the sole current responder. These are no longer pending questions. Legal retention and later production/provider decisions remain separate gates.

Do not equate these local implementation notes with staging acceptance, a repaired customer artifact, or a production go-live approval. No customer document or historical legal record has been overwritten by this pass.
